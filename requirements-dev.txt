# Development / test dependencies for STAR SARA.
# The unit tests stub the heavy runtime deps (whisper, PySide6, sounddevice,
# pygame, edge-tts, groq) so they are NOT required to run the suite.
pytest>=8.0.0
pytest-cov>=5.0.0
numpy>=1.24.0
rapidfuzz>=3.6.0
